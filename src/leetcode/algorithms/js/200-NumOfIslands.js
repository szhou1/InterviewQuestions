// Input:
// 11110
// 11010
// 11000
// 00000

// visited
// vvvv0
// vv0v0
// vv000
// 00000

// Output: 1

// Input:
// 11000
// 11000
// 00100
// 00011

// vv000
// vv000
// 00v00
// 000vv

// Output: 3

/**
 * @param {character[][]} grid
 * @return {number}
 */
var numIslands = function(grid) {
    //  var islands = 0
    //  for each row
    //      for each col in row
    //          if 1
    //              dfs(row, col, grid)
    //              islands++
    // return islands
};

var dfs = function (row, col, grid) {
    // if row < 0 || col < 0 || row > nr || col > nc, then return
    //  grid[row][col] = 0;
    //  dfs(row - 1, col, grid)
    //  dfs(row, col - 1, grid)
    //  dfs(row + 1, col, grid)
    //  dfs(row, col + 1, grid)
}

